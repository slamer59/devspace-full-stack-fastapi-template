apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: fastapi-backend
    component: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: fastapi-backend